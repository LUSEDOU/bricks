name: firebase_analytics_client
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.51 <0.1.0"

vars:
  path:
    type: string
    description: The path to the analytics_client.
    default: ../analytics_client
    prompt: What is the path to the analytics_client?
